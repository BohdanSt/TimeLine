<UserControl x:Class="TimeLine.GamesRules"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:Controls="http://metro.mahapps.com/winfx/xaml/controls"
             xmlns:local="clr-namespace:TimeLine"
             mc:Ignorable="d" 
             d:DesignHeight="500" d:DesignWidth="750">
    <Grid>
        <Grid.Background>
            <ImageBrush ImageSource="pack://application:,,,/TimeLine;component/Images/BackgroundRules.png" 
                        Stretch="UniformToFill" RenderOptions.BitmapScalingMode="HighQuality"/>
        </Grid.Background>
        <Grid.RowDefinitions>
            <RowDefinition/>
            <RowDefinition Height="auto"/>
        </Grid.RowDefinitions>
        <RichTextBox x:Name="richTextBoxRules" Grid.Row="0" Margin="10" Background="Transparent" BorderThickness="0"
                     HorizontalContentAlignment="Stretch" VerticalContentAlignment="Center" FontSize="16"/>
        <Button x:Name="buttonStartGame" Grid.Row="1" Content="Розпочати гру" Margin="50" Height="60" Width="450" FontSize="16">
            <Button.Background>
                <SolidColorBrush Color="WhiteSmoke" Opacity="0.75"/>
            </Button.Background>
        </Button>
    </Grid>
</UserControl>
